services:
  - type: web
    name: skype-revival-backend
    runtime: docker
    plan: free
    healthCheckPath: /health
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: DB_PATH
        value: /app/data/skype_revival.db
